#!/usr/bin/env python3

# Define a list of words
words = ['cat', 'window', 'Avacardo', 'defenestrate', 'hippopotamus',]

# Iterate through each word in the list
for w in words:
    # Print the word and its length
    print(w, len(w))
